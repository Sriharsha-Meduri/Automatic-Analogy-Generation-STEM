# LaTeX build artifacts (the compiled paper/main.pdf is kept; these regenerate on build)
paper/*.aux
paper/*.log
paper/*.out
paper/*.blg
paper/*.bbl
paper/*.synctex.gz
paper/compile*.log
paper/bibtex.log

# Python
__pycache__/
*.pyc

# stray local download logs (not research artifacts)
ollama_pull.log
pull2.log
serve*.log

# arXiv submission staging folder (redundant with paper/ + figures/; the .zip is kept)
arxiv_submission/
